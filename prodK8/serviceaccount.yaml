apiVersion: v1
kind: ServiceAccount
metadata:
  name: twc-recommendations
  namespace: recommendations
  annotations:
    # Uncomment and update if using IRSA for AWS access (e.g., Secrets Manager)
    # eks.amazonaws.com/role-arn: arn:aws:iam::123456789012:role/twc-recommendations-role
