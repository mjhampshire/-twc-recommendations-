apiVersion: v1
kind: Service
metadata:
  name: twc-recommendations
  namespace: recommendations
  labels:
    app: twc-recommendations
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
  selector:
    app: twc-recommendations
